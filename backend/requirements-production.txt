# Optional services for a deployment or multi-user server. Not required for
# local-only use; no deployment is part of this project pass.
asyncpg==0.30.0
redis==5.0.3
qdrant-client>=1.12.0
huggingface_hub>=0.34,<1.0
